# Render static site for the `better` frontend
services:
  - type: web
    name: azura-ai-systems
    runtime: static
    buildCommand: "pnpm install && pnpm build"
    staticPublishPath: "dist"
    branch: main