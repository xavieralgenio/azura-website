# Render static site for the `better` frontend
services:
  - type: static
    name: azura-better
    env: static
    buildCommand: "pnpm install && pnpm build"
    publishPath: "dist"
    branch: main
